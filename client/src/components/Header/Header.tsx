import React from 'react';
import { AppHeader, HeaderSection} from './styles';

const Header: React.FC = () => {

    return (
        <AppHeader>
            <HeaderSection>
                ТЕЛЕФОННАЯ КНИГА
            </HeaderSection>
        </AppHeader>
    );
};

export default Header;
